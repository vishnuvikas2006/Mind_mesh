import Link from "next/link";
import { ArrowRight, HeartPulse } from "lucide-react";
import { StatusBadge } from "./StatusBadge";
import type { Risk } from "@/lib/api";
export function WellbeingSummaryCard({ risk, checkins }: { risk: Risk | null; checkins: number }) { const progress = risk ? risk.dynamic_score : 0; return <article className="rounded-[24px] border border-[#e4eeee] bg-white p-5 shadow-calm"><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-[#ddf3ee] text-deepteal"><HeartPulse className="h-5 w-5" /></span><div><p className="font-semibold text-ink">{risk ? "Recent support signal" : "Your wellbeing"}</p><p className="mt-1 text-sm text-body">{risk ? "A support signal, not a diagnosis." : "Check-ins will appear here."}</p></div></div>{risk && <StatusBadge level={risk.risk_level} />}</div>{risk ? <><div className="mt-5 h-2 overflow-hidden rounded-full bg-[#e9f3f1]" role="progressbar" aria-label="Recent support signal" aria-valuenow={progress} aria-valuemin={0} aria-valuemax={100}><div className="h-full rounded-full bg-teal" style={{ width: `${Math.max(7, progress)}%` }} /></div><p className="mt-3 text-sm leading-5 text-body">High-priority signals are reviewed by a person.</p></> : <p className="mt-5 text-sm leading-5 text-body">{checkins} check-in{checkins === 1 ? "" : "s"} completed.</p>}<Link href="/wellbeing" className="mt-4 inline-flex items-center gap-1 text-sm font-semibold text-deepteal hover:underline">View history <ArrowRight className="h-4 w-4" /></Link></article>; }
