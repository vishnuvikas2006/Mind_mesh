import Link from "next/link";
import { ArrowRight, CalendarDays, MessageCircleHeart } from "lucide-react";
import { StatusBadge } from "./StatusBadge";
export type SupportSession = { _id: string; support_type: string; scheduled_for: string; status: string };
export function UpcomingSupportCard({ session }: { session?: SupportSession }) { return <article className="rounded-[24px] border border-[#e4eeee] bg-white p-5 shadow-calm">{session ? <Link href="/support" className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-[#fff4d6] text-[#8a6518]"><CalendarDays className="h-5 w-5" /></span><span className="min-w-0 flex-1"><span className="block font-semibold text-ink">{session.support_type}</span><span className="mt-1 block text-sm text-body">{new Date(session.scheduled_for).toLocaleString([], { dateStyle: "medium", timeStyle: "short" })}</span></span><StatusBadge level="scheduled" /><ArrowRight className="h-4 w-4 text-deepteal" /></Link> : <div className="flex items-start gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-2xl bg-[#e6f2f5] text-deepteal"><MessageCircleHeart className="h-5 w-5" /></span><div><p className="font-semibold text-ink">No upcoming sessions</p><p className="mt-1 text-sm leading-5 text-body">Explore support when you are ready.</p><Link href="/help" className="mt-3 inline-flex text-sm font-semibold text-deepteal hover:underline">Explore support</Link></div></div>}</article>; }
